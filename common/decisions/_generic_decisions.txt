
political_actions = {
	
	improved_worker_conditions = {

		icon = generic_industry
		
			allowed = { normally_played_country = yes 	}

		available = {
			has_stability < 1.0
		}

		visible = {
			always = yes
		}

		fire_only_once = no

		modifier = {
			stability_weekly = 0.005
			consumer_goods_factor = 0.05
			industrial_capacity_factory = -0.1
		}

		days_remove = 180
		days_re_enable = 540

		cost = 100

		complete_effect = {
		custom_effect_tooltip = "Gives 12.85% Stability in total.  Decision will be available again in 720 days."
		}

		ai_will_do = {
			base = 0
			modifier = {
				add = 1
				has_stability < 0.6
			}
		}
	}

	promises_of_peace = {

		icon = generic_political_discourse

		available = {
			has_war = no
			has_war_support > 0.2
			has_stability < 1.0
		}
			allowed = { normally_played_country = yes 	}


		visible = {
			always = yes
		}

		remove_trigger = {
			has_war = yes
			has_war_support < 0.05
		}

		fire_only_once = no

		modifier = {
			stability_weekly = 0.005
			war_support_weekly = -0.01
			political_power_gain = -0.5
		}

		days_remove = 90
		days_re_enable = 360

		cost = 50

		complete_effect = {
		custom_effect_tooltip = "Gives 6.42% Stability and -12.85% War Support in total.  Decision will be available again in 450 days."
		}

		ai_will_do = {
			base = 0
			modifier = {
				add = 1
				has_stability < 0.4
			}
		}
	}
}

propaganda_effortsx = {
		war_propaganda = {
		icon = generic_prepare_civil_war
		allowed = {	normally_played_country = yes 	}		
		available = {
				NOT = { has_country_flag = war_propaganda_campaign_running }
				threat > 0.1
			}
		fire_only_once = no
		days_remove = 90
		days_re_enable = 0
		cost = 100
		modifier = {	war_support_weekly = 0.01	}
		complete_effect = {
				custom_effect_tooltip = "Gives 12.85% War Support in total. Decision will be available again in 90 days."
			set_country_flag = war_propaganda_campaign_running
		}
		remove_effect = {
			clr_country_flag = war_propaganda_campaign_running
		}
	}
}

operations = {
	blow_suez_canal = {
		icon = GFX_decision_generic_ignite_civil_war
		allowed = {
			has_dlc = "Waking the Tiger"
		}
			allowed = { 
		
	OR = {	
		tag = JAP
		tag = MAN
		tag = EFR 
		tag = ROM
		tag = HUN
		tag = SPR
		tag = GER
		tag = ITA
		tag = ENG
		tag = FIN  }
		}
		
		
		available = {
			controls_state = 446
			controls_state = 453
			OR = {
				NOT = {
					owns_state = 446
					owns_state = 453
				}
				any_country = {
					has_war_with = ROOT
					OR = {
						controls_state = 452
						controls_state = 447
						controls_state = 456
						controls_state = 457
						controls_state = 454
						controls_state = 455
					}
				}
			}
		}
	
		visible = {
			has_war = yes
			NOT = { has_global_flag = SUEZ_CANAL_BLOCKED }
			any_state = {
				is_controlled_by = ROOT
				OR = {
					region = 28
					region = 128
				}
			}
		}
	
		fire_only_once = no
	
		days_remove = 30
	
		cost = 75
	
		modifier = {
			civilian_factory_use = 5
		}

		complete_effect = {
			hidden_effect = {
				if = {
					limit = {
						NOT = {
							owns_state = 446
							owns_state = 453
						}
					}
					random_country = {
						limit = {
							owns_state = 446
							owns_state = 453
						}
						country_event = { id = generic.12 days = 1 }
					}
				}
			}
		}
	
		remove_effect = {
			if = {
				limit = {
					OR = {
						has_full_control_of_state = 446
						has_full_control_of_state = 453
					}
				}
				set_country_flag = blew_up_suez
				set_global_flag = SUEZ_CANAL_BLOCKED
				country_event = { id = wtt_news.40 hours = 6}
			}
			effect_tooltip = {
				if = {
					limit = {
						ENG = {
							owns_state = 446
							owns_state = 453
						}
					}
					add_war_support = -0.05
					add_stability = -0.05
				}
			}
		}
	}
	

}

political_actions = {
	rebuild_suez_canal = {
		icon = GFX_decision_generic_special_project

	allowed = { 
	OR = { 	
		tag = JAP
		tag = MAN
		tag = ROM
		tag = HUN
		tag = SPR
		tag = GER
		tag = ITA
		tag = FIN 
		tag = ENG 
		tag = USA
		}
		}
		
		available = {
			has_full_control_of_state = 446
			has_full_control_of_state = 453
			num_of_civilian_factories > 15
		}
	
		visible = {
			has_global_flag = SUEZ_CANAL_BLOCKED
		}
	
		fire_only_once = no
	
		days_remove = 365
	
		cost = 120
	
		modifier = {
			civilian_factory_use = 15
		}
	
		remove_effect = {
			if = {
				limit = {
					has_full_control_of_state = 446
					has_full_control_of_state = 453
				}
				set_country_flag = rebuilt_suez
				clr_global_flag = SUEZ_CANAL_BLOCKED
				country_event = { id = wtt_news.42 hours = 6}
			}
		}
	}
}

propaganda_effortsx = {
	
	emergency_factory_conversion_defensive = {

		icon = generic_industry
		
			allowed = { normally_played_country = yes 	}

		available = {
			num_of_civilian_factories > 5 #Change this if amount of factories change
		}

		visible = { has_war = yes	}

		fire_only_once = no

		days_re_enable = 90

		cost = 100

		complete_effect = {
			replace_civ_with_arms_factories = yes
			add_war_support = -0.05
		}
	}

	desperate_defense = {

		icon = generic_prepare_civil_war

		available = {	surrender_progress > 0.5	}
		
			allowed = { 
	OR = { 	
		tag = EFR 
		tag = RAJ
		tag = AST
		tag = NZL
		tag = GER
		tag = ITA
		tag = ROM
		tag = HUN
		tag = FRA
		tag = SPR
		tag = ENG
		tag = SAF
		tag = FIN 
		tag = USA
		tag = CAN
		tag = MEX
		tag = NOR  
			}
		}
		visible = { surrender_progress > 0.2 }
		fire_only_once = no
		days_remove = 45
		days_re_enable = 360
		cost = 50
		modifier = {
			defence = 0.2
			dig_in_speed_factor = 0.5
		}
		complete_effect = {
		}
	}

	women_in_the_workforce = {

		icon = generic_industry

	allowed = { normally_played_country = yes 	}

        available = {
            has_idea = tot_economic_mobilisation
            OR = {
                AND = {
                   NOT = {  has_government = fascism }
                    has_war_support > 0.79
                }
                OR = {
                    has_war_support > 0.89
                    surrender_progress > 0
                }
            }
        }
        visible = {    has_war = yes   }
        cost = 100
        days_remove = -1
        remove_trigger = {	not = { has_idea = tot_economic_mobilisation }         }
		modifier = {
            conscription = 0.03
            stability_factor = -0.05
        }

		complete_effect = {

        }
   }

    war_bonds = {

    	icon = generic_industry

    	allowed = { normally_played_country = yes	}
        available = {
            OR = {
                AND = {
                NOT = {  has_government = fascism }
				has_war_support > 0.79
				}
                AND = {
                    has_government = fascism
                    OR = {
                        has_war_support > 0.89
                        surrender_progress > 0
                    }
                }
            }
			NOT = { has_idea = zzz_war_bonds }
        }
        visible = {
			
            has_war = yes
			NOT = { has_completed_focus = GER_jag }
        }

        fire_only_once = no

		days_remove = 180

		cost = GER_war_bonds_var?50

		modifier = {
			
		}

		remove_effect = {

		}

		complete_effect = {
			ROOT = { add_timed_idea = { idea = zzz_war_bonds days = 180 } }
			if = {
				limit = {
					check_variable = {
						var = GER_war_bonds_var
						value = 50
						compare = less_than
					}
				}
				set_variable = {
					var = GER_war_bonds_var
					value = 50
				}
			}
			add_to_variable = {
				var = GER_war_bonds_var
				value = 25
			}
			if = {
				limit = {
					check_variable = {
						var = GER_war_bonds_var
						value = 100
						compare = greater_than_or_equals
					}
					check_variable = {
						var = GER_war_bonds_war_support_var
						value = -0.025
						compare = greater_than
					}
				}
				set_variable = {
					var = GER_war_bonds_war_support_var
					value = -0.025
				}
			}
			if = {
				limit = {
					check_variable = {
						var = GER_war_bonds_war_support_var
						value = -0.025
						compare = less_than_or_equals
					}
				}
				add_to_variable = {
					var = GER_war_bonds_war_support_var
					value = -0.025
				}
				add_war_support = GER_war_bonds_war_support_var
			}
		}

    }
}

political_actions = {
	infrastructure_building_slot = {

		icon = generic_construction

      	allowed = { 
		OR = { 
		tag = SOV
		tag = GER
		tag = ITA
		tag = JAP
		tag = EFR 
		tag = RAJ
		tag = AST
		tag = NZL
		tag = FRA
		tag = ROM
		tag = MAN
		tag = HUN
		tag = SPR
		tag = ENG
		tag = SAF
		tag = USA
		tag = CAN
		tag = MEX
		tag = NOR 
		}
		}

        cost = 100
        fire_only_once = no

        days_re_enable = 30

	available = {
		any_owned_state = {
			NOT = { has_state_flag = infrastructure_building_slot_flag }
			is_fully_controlled_by = ROOT
			free_building_slots = {
				building = infrastructure
				size < 1
			}
		}
	}

	visible = {
		any_owned_state = {
			NOT = { has_state_flag = infrastructure_building_slot_flag }
			is_fully_controlled_by = ROOT
			free_building_slots = {
				building = infrastructure
				size < 1
			}
		}
	}

        complete_effect = {
			random_owned_state = {
				limit = {
					is_fully_controlled_by = ROOT
					is_owned_by = ROOT
            		free_building_slots = {
						building = industrial_complex
						size > 1
						include_locked = yes
					}
					free_building_slots = {
						building = infrastructure
						size < 1
					}
					NOT = { has_state_flag = infrastructure_building_slot_flag }
				}
				add_extra_state_shared_building_slots = 2
				set_state_flag = infrastructure_building_slot_flag
			}
		}
		ai_will_do = {
			factor = 1
			modifier = {
				factor = 10
				any_owned_state = { free_building_slots = { building = industrial_complex size < 1 include_locked = no } }
			}
		}
    }

    war_building_slot = {

    	icon = generic_construction

        cost = 50
        fire_only_once = no

        available = {
            has_war = yes
            any_owned_state = {
            	NOT = { has_state_flag = war_building_slot_flag }
            	is_fully_controlled_by = ROOT
            }
        }
		
			allowed = { 
		OR = { 
		tag = SOV
		tag = FRA
		tag = RAJ
		tag = AST
		tag = NZL
		tag = ROM
		tag = HUN
		tag = EFR 
		tag = SPR
		tag = ENG
		tag = SAF
		tag = FIN 
		tag = USA
		tag = CAN
		tag = MAN
		tag = MEX
		tag = NOR 
		}
		}

        visible = {
			has_war = yes
			any_owned_state = {
				NOT = { has_state_flag = war_building_slot_flag }
				is_fully_controlled_by = ROOT
			}
		}

        complete_effect = {
			if = {
				limit = {
					any_owned_state = {
						is_fully_controlled_by = ROOT
            			free_building_slots = {
							building = industrial_complex
							size > 1
							include_locked = yes
						}
						NOT = { has_state_flag = war_building_slot_flag }
					}
				}
				random_owned_state = {
					limit = {
						is_fully_controlled_by = ROOT
            			free_building_slots = {
							building = industrial_complex
							size > 1
							include_locked = yes
						}
						NOT = { has_state_flag = war_building_slot_flag }
					}
					add_extra_state_shared_building_slots = 1
					set_state_flag = war_building_slot_flag
				}
			}
		}

    }
	
	
    war_building_slot2 = {

    	icon = generic_construction

        cost = 75
        fire_only_once = no

        available = {
            any_owned_state = {
            	is_fully_controlled_by = ROOT
            }
        }
		
		fixed_random_seed = no
		
			allowed = { 
		OR = { 
			tag = EFR 
		tag = RAJ
		tag = AST
		tag = NZL
		tag = ROM
		tag = HUN
		tag = SPR
		tag = SAF
		tag = FIN 
		tag = CAN
		tag = MEX
		tag = NOR }
		}

        visible = {
			any_owned_state = {
				is_fully_controlled_by = ROOT
			}
		}

        complete_effect = {
			random_owned_state = {
			add_extra_state_shared_building_slots = 1
				}
			}
		}

    }